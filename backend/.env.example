# Server configuration
PORT=3001
ADMIN_TOKEN=replace_with_a_long_random_token
ADMIN_NAME=站长

# Optional test/development path overrides.
# Leave these unset in production unless the deployment intentionally uses
# isolated paths. COMMENT_ENV_FILE should point only to a non-secret test file.
# COMMENT_ENV_FILE=
# COMMENT_DATA_DIR=
# COMMENT_DB_PATH=
# COMMENT_UPLOADS_DIR=
